use std::collections::BinaryHeap;
use std::io::{BufRead, Lines};

use common::Input;

pub fn solve(input: &Input) -> i64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn simple() {}
}
