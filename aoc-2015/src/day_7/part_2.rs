use std::io::BufRead;

use common::Input;

pub fn solve(input: &Input) -> i64 {
    0
}

#[cfg(test)]
mod tests {

    use super::*;

    use common::{check, Checker};

    const CHECKER: Checker = Checker::new(solve);

    #[test]
    fn simple() {}
}
